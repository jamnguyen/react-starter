import { Route } from '~src/interfaces';

export const ROUTES: Record<string, Route> = {
  HOME: {
    path: '/',
    name: 'Home',
  },
  STYLE_GUIDE: {
    path: '/style-guide',
    name: 'Style Guide',
  },
};
